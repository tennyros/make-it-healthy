version: '3.8'
services:
  make-it-healthy-db:
    image: postgres:17-alpine
    container_name: make-it-healthy-db
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5433:5432"